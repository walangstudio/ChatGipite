{
  "tools": [
    {
      "name": "biz_generate",
      "description": "Generate business ideas based on optional sector, problem statement, or constraints. Returns structured idea briefs with problem/solution/target/hooks.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "sector": { "type": "string", "description": "Industry or sector (e.g. healthtech, edtech, fintech, SaaS, consumer)" },
          "problem": { "type": "string", "description": "A problem or pain point to build around" },
          "constraints": { "type": "string", "description": "Constraints like budget, solo founder, B2B only, no-code, etc." },
          "count": { "type": "integer", "minimum": 1, "maximum": 10, "default": 1, "description": "Number of ideas to generate (1-10, default: 1)" }
        }
      }
    },
    {
      "name": "biz_validate",
      "description": "Run a full viability analysis on a given idea: problem clarity, ICP, solution fit, revenue model options, market size estimate, and ICE score.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea": { "type": "string", "minLength": 3, "description": "The business idea to validate" },
          "context": { "type": "string", "description": "Additional context about the idea, target market, or constraints" }
        },
        "required": ["idea"]
      }
    },
    {
      "name": "biz_ice_score",
      "description": "Compute an ICE score (Impact 1-10, Confidence 1-10, Ease 1-10) for a business idea with rationale for each dimension.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea": { "type": "string", "minLength": 3, "description": "The business idea to score" },
          "context": { "type": "string", "description": "Additional context" }
        },
        "required": ["idea"]
      }
    },
    {
      "name": "biz_canvas",
      "description": "Generate a strategy canvas for a saved idea. canvas_type selects: bmc (Business Model Canvas, default — for funded/operating), lean (Lean Canvas, Running Lean 3rd ed — for unproven ideas), vpc (Value Proposition Canvas — demand-side fit), mission (Mission Model Canvas — mission-driven/non-profit), ai-platform (AI Canvas + Platform Canvas — model-core or multi-sided).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea (lowercase letters, digits, hyphens only; e.g. ai-meal-planning)" },
          "canvas_type": { "type": "string", "enum": ["bmc", "lean", "vpc", "mission", "ai-platform"], "default": "bmc", "description": "Which canvas to produce (default: bmc)" }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_lean_canvas",
      "description": "Lean Canvas (Running Lean 3rd ed): Problem (+Existing Alternatives) → Customer Segments (+Early Adopters) → UVP (+High-Level Concept) → Solution → Channels → Revenue → Cost → Key Metrics (Customer Factory) → Unfair Advantage. Best for unproven, pre-revenue ideas.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea" }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_value_prop",
      "description": "Value Proposition Canvas: customer profile (jobs/pains/gains) mapped to value map (products/pain relievers/gain creators) per priority segment, plus a problem–solution fit assessment. Pulls personas if available.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea" }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_mission_canvas",
      "description": "Mission Model Canvas: BMC variant for mission-driven, non-profit, or government ventures (beneficiaries, buy-in/support, deployment, mission budget, impact factors instead of revenue/profit).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea" }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_ai_canvas",
      "description": "AI Canvas (prediction, judgment, action, outcome, input, training data, feedback loop) and/or Platform Canvas (sides, value unit, network effects, chicken-and-egg, governance) for model-core or multi-sided-market ideas.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea" }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_pitchdeck",
      "description": "Generate a 10-slide pitch deck (Problem, Solution, Market, Product, Business Model, Traction, Competition, Team, Financials, Ask) for a saved idea.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea" }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_name",
      "description": "Generate business name options for an idea with branding score (memorability, pronounceability, uniqueness).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea" },
          "style": { "type": "string", "description": "Naming style: coined, descriptive, metaphor, acronym, portmanteau" },
          "count": { "type": "integer", "minimum": 1, "maximum": 20, "default": 8, "description": "Number of names to generate (1-20, default: 8)" }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_name_check",
      "description": "Check brand-name availability: domains via RDAP→WHOIS→DNS (default com,io,ai,co,app,dev,net,org) and social handles (X, Instagram, TikTok, LinkedIn, GitHub, YouTube, Facebook, Reddit, Threads).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": { "type": "string", "minLength": 1, "maxLength": 60, "description": "Business name to check (e.g. NutriAI, Validea)" },
          "tlds": { "type": "array", "items": { "type": "string" }, "description": "TLDs to check without dots (e.g. [\"com\",\"io\",\"ai\"]). Defaults to com,io,ai,co,app,dev,net,org." },
          "socials": { "type": "array", "items": { "type": "string" }, "description": "Socials to check: x, instagram, tiktok, linkedin, github, youtube, facebook, reddit, threads, or [\"all\"]. Defaults to all." }
        },
        "required": ["name"]
      }
    },
    {
      "name": "biz_competitors",
      "description": "List direct and indirect competitors for an idea, analyze their strengths/weaknesses, and identify differentiation opportunities.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea" },
          "market": { "type": "string", "description": "Optional market focus (e.g. US, APAC, SMB, enterprise)" }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_financials",
      "description": "Model unit economics, CAC/LTV ratio, gross margin, break-even timeline, and 12-month P&L projection.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea" },
          "assumptions": {
            "type": "object",
            "description": "Override assumptions",
            "properties": {
              "price_per_user": { "type": "number" },
              "monthly_churn_pct": { "type": "number" },
              "cac_estimate": { "type": "number" },
              "initial_users": { "type": "number" },
              "monthly_growth_pct": { "type": "number" }
            }
          }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_playbook",
      "description": "Generate a 30/60/90-day execution playbook with milestones, KPIs, resource requirements, and risk flags.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea" }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_full_run",
      "description": "Run the full end-to-end analysis pipeline: validate → canvas → competitors → financials → names → playbook. Saves all artifacts to ideas/{slug}/.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea": { "type": "string", "minLength": 3, "description": "The business idea to fully analyze" },
          "sector": { "type": "string", "description": "Optional sector context" },
          "constraints": { "type": "string", "description": "Optional constraints (solo founder, bootstrapped, etc.)" }
        },
        "required": ["idea"]
      }
    },
    {
      "name": "biz_tam",
      "description": "Deep TAM/SAM/SOM sizing using both top-down and bottom-up methodology. Produces addressable market estimates with geographic breakdowns.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea" },
          "geography": { "type": "string", "description": "Geographic scope (e.g. US, Global, APAC, Europe)" }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_personas",
      "description": "Generate 3-5 customer personas with Jobs-to-be-Done framing, ICP prioritization, and willingness-to-pay estimates per segment.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea" },
          "segment_focus": { "type": "string", "description": "Optional: focus on a specific segment (e.g. SMB, enterprise, consumers)" }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_trends",
      "description": "Analyze industry trends using PESTLE + technology S-curves. Maps tailwinds, headwinds, and market timing window across 1/3/5-year horizons.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea" },
          "horizon": { "type": "string", "enum": ["1yr", "3yr", "5yr"], "default": "3yr", "description": "Analysis horizon (default: 3yr)" }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_swot",
      "description": "SWOT analysis + Porter's Five Forces (each force rated 1-5) with strategic cross-analysis (SO/WO/ST/WT strategies).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea" }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_pricing",
      "description": "Design a 3-tier pricing architecture with model selection, competitor price table, value metric, and expansion revenue paths.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea" },
          "model_preference": { "type": "string", "description": "Optional pricing model preference (e.g. usage-based, seat-based, freemium)" }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_gtm",
      "description": "Go-to-market strategy: GTM motion, beachhead segment, 0→100 and 100→1000 channels, positioning statement, and 30-day launch plan.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea" },
          "stage": { "type": "string", "description": "Current stage: pre-launch, launched, or scaling" }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_journey",
      "description": "Map the 7-stage customer journey (Awareness → Advocacy) with moments of truth, drop-off risks, and delight opportunities.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea" },
          "persona": { "type": "string", "description": "Optional: focus on a specific persona (references personas artifact if available)" }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_landscape",
      "description": "Deep competitive intelligence: funding trajectories, moat analysis, white-space map, positioning grid, and competitive response scenarios.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea" },
          "market": { "type": "string", "description": "Optional market focus (e.g. US, APAC, SMB, enterprise)" }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_model",
      "description": "3-scenario financial model (base/upside/downside) with monthly P&L, cohort retention, sensitivity analysis, and funding requirements.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea" },
          "assumptions": {
            "type": "object",
            "description": "Override base-case assumptions",
            "properties": {
              "price_per_user": { "type": "number" },
              "monthly_churn_pct": { "type": "number" },
              "cac_estimate": { "type": "number" },
              "initial_users": { "type": "number" },
              "monthly_growth_pct": { "type": "number" }
            }
          }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_risks",
      "description": "Risk register (probability × impact), 3-scenario narratives (base/upside/downside), regulatory flags, and 90-day assumption validation tests.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea" }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_expansion",
      "description": "Market entry mode analysis, 3-phase geographic/vertical expansion roadmap, localization requirements, and measurable go/no-go criteria per phase.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea" },
          "target_markets": { "type": "string", "description": "Optional: specific markets to analyze (e.g. EU, APAC, LATAM)" }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_synthesis",
      "description": "Executive synthesis: reads all available artifacts and produces a board-level brief with conviction score (1-10), 5 key decisions, and 90-day action roadmap.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea_slug": { "type": "string", "pattern": "^[a-z0-9-]+$", "minLength": 1, "maxLength": 80, "description": "Slug of the saved idea" }
        },
        "required": ["idea_slug"]
      }
    },
    {
      "name": "biz_deep_run",
      "description": "Full deep-analysis pipeline (15-step workflow + names): validate → personas → trends → market → tam → financials → model → swot → pricing → gtm → journey → risks → landscape → expansion → synthesis → names. Saves 16 artifacts to ideas/{slug}/.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "idea": { "type": "string", "minLength": 3, "description": "The business idea to fully analyze" },
          "sector": { "type": "string", "description": "Optional sector context" },
          "constraints": { "type": "string", "description": "Optional constraints (solo founder, bootstrapped, etc.)" }
        },
        "required": ["idea"]
      }
    },
    {
      "name": "biz_recall",
      "description": "Full-text search across all stored ideas, analyses, and artifacts.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": { "type": "string", "minLength": 1, "description": "Search query" },
          "limit": { "type": "integer", "minimum": 1, "maximum": 50, "default": 10, "description": "Max results (1-50, default: 10)" },
          "type": { "type": "string", "description": "Filter by type: ideas, validations, financials, playbooks, names, tam, personas, trends, swot, pricing, gtm, journey, landscape, model, risks, expansion, synthesis" }
        },
        "required": ["query"]
      }
    }
  ]
}
